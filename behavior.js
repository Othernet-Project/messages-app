/**
 * Icon behavior module
 *
 * This script is inserted into the app list, just below the HTML for the app
 * icon for this particular app. When this script runs, we can safely assume
 * the HTML for the app icon is present, but not much else.
 */

(function (window) {
  // NOT IMPLEMENTED
  if (console && console.log) {
    console.log('Hello from icon behavior');
  }
}(this));
